import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - Smart Notebook',
  description: 'Sign in to your Smart Notebook account',
};

export default function LoginPage() {
  return <LoginForm />;
}
