import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - Smart Notebook',
  description: 'Create a new Smart Notebook account',
};

export default function SignupPage() {
  return <SignupForm />;
}
